# .goreleaser.yaml — esp-tool release configuration
# Docs: https://goreleaser.com/customization/
version: 2

project_name: esp-tool

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: esp-tool
    main: ./cmd/esp-tool
    binary: esp-tool
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    # arm64 is not a supported Windows target in Go's standard toolchain.
    ignore:
      - goos: windows
        goarch: arm64
    # Embed version info from the git tag at build time.
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    # Archive format: zip on Windows, tar.gz everywhere else.
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    # Include the zsh completion script in non-Windows archives.
    files:
      - completions/_esp-tool
      - README.md
      - LICENSE
    # Name template: esp-tool_v1.2.3_darwin_arm64.tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
  groups:
    - title: Features
      regexp: "^feat"
      order: 0
    - title: Bug Fixes
      regexp: "^fix"
      order: 1
    - title: Other Changes
      order: 999

release:
  github:
    owner: gevgev
    name: esp-tool
  # Include the completions directory as an extra artifact.
  extra_files:
    - glob: completions/_esp-tool
  # Draft releases let you review before publishing.
  draft: false
  prerelease: auto
  footer: |
    ## Installation

    Download the archive for your platform, extract it, and move `esp-tool` to a location on your `PATH`:

    ```bash
    # macOS (Apple Silicon)
    curl -sL https://github.com/gevgev/esp-tool/releases/download/{{ .Tag }}/esp-tool_{{ .Version }}_darwin_arm64.tar.gz | tar -xz
    sudo mv esp-tool /usr/local/bin/

    # macOS (Intel)
    curl -sL https://github.com/gevgev/esp-tool/releases/download/{{ .Tag }}/esp-tool_{{ .Version }}_darwin_amd64.tar.gz | tar -xz
    sudo mv esp-tool /usr/local/bin/

    # Linux (amd64)
    curl -sL https://github.com/gevgev/esp-tool/releases/download/{{ .Tag }}/esp-tool_{{ .Version }}_linux_amd64.tar.gz | tar -xz
    sudo mv esp-tool /usr/local/bin/

    # Windows (amd64) — PowerShell
    Invoke-WebRequest -Uri https://github.com/gevgev/esp-tool/releases/download/{{ .Tag }}/esp-tool_{{ .Version }}_windows_amd64.zip -OutFile esp-tool.zip
    Expand-Archive esp-tool.zip -DestinationPath .
    Move-Item esp-tool.exe $env:USERPROFILE\AppData\Local\Microsoft\WindowsApps\
    ```

    **Note:** On Windows, `esphome` is typically only available inside WSL2.
    The native Windows binary works if `esphome` is on your `PATH`; otherwise
    use the Linux build from within WSL2.

    See the [README](https://github.com/gevgev/esp-tool#readme) for full setup instructions including zsh completion.
